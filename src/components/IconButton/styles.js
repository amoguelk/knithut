import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    alignItems: "center",
    margin: 10,
  },
  icon: {
    width: 60,
    height: 60,
  },
  label: {
    color: "#fff",
    fontSize: 16,
  },
});
